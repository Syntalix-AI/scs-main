export const metadata = {
  title: "Our Team | Syntalix",
  description: "Meet the specialized divisions at Syntalix, including Leadership, AI Engineering, and Web & Mobile Development.",
};

export default function TeamLayout({ children }) {
  return <>{children}</>;
}
